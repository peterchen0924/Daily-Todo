本週學習：

1.自主權:我在主導自己的生活嗎?
2.精通感:我在某見重要的事情上變得更好了嗎?
3.使命感:我的工作是否為別人帶來了改變或貢獻?

int   一個數字
int*  一維陣列（或單一指標）
int** 二維陣列
char** 一堆字串

char** answer = malloc(n * sizeof(char*)); // 配置 n 個 char* 的空間（n 個字串的位址）
answer[i] = malloc(12 * sizeof(char)); // 配置單一字串的空間（最多存 12 個字元）
char buf[12];
sprintf(buf, "%d", 123);  // buf 現在是 "123"，sprintf 印到字串裡

current = current->next; //節點往下一個節點
鏈表回傳節點會自動接著

本週目標：